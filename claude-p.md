*Replace the system prompt*
- `claude -p "prompt" --system-prompt "your custom system prompt"` — replaces it entirely
- `claude -p "prompt" --append-system-prompt "extra instructions"` — appends to the default

Note this is separate from CLAUDE.md files, which layer in as additional context rather than replacing the system prompt.
